/**
 * Optional Supabase client for the marketing site's lead form (Option B).
 * See docs/PLAN.md Section 7 — use only if lead submissions need to persist.
 * For Option A (no backend / form service), this file can remain unused.
 *
 * Uncomment and set NEXT_PUBLIC_SUPABASE_URL / NEXT_PUBLIC_SUPABASE_ANON_KEY
 * when a Supabase project is provisioned.
 */

// import { createClient } from "@supabase/supabase-js";

// export const supabase =
//   process.env.NEXT_PUBLIC_SUPABASE_URL && process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY
//     ? createClient(
//         process.env.NEXT_PUBLIC_SUPABASE_URL,
//         process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY
//       )
//     : null;
